# Coding agents

Jewel Tooling can expose the same static Compose stability analysis to a coding agent
running against your open project. It uses the editor engine. You do not launch the
application, add a build plugin, or change a dependency.

Open **Tools → Compose Analysis MCP Server…**, enable the server, select a coding client,
and click **Install**.

| Page | Use it to |
| --- | --- |
| [MCP server](mcp.md) | Enable access, install a client, and call the four analysis tools |
| [Agent skill](skill.md) | Install `jewel-compose-analysis` so the agent cites evidence correctly |
| [Workflows](workflows.md) | Copy a short instruction block into a consuming project |

Pi support uses **pi-mcp-adapter**. If you use another extension, ask your agent to adapt
the setup.
